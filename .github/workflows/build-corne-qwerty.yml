name: 'Build Example crkbd'
on:
  - workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    with:
      keyboard: '["crkbd"]'
      clipboard: '["MAC"]'
      nav: '["VI"]'
      alphas: '["QWERTY"]'
